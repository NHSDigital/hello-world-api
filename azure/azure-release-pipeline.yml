name: "$(SourceBranchName)+$(BuildID)"

trigger: none
pr: none

resources:
  repositories:
    - repository: common
      type: github
      name: NHSDigital/api-management-utils
      ref: refs/heads/APM-1318-Add-sandbox-to-friendly-names
      endpoint: NHSDigital
  pipelines:
  - pipeline: build_pipeline
    source: "Hello-World-Build"
    trigger:
      branches:
        include:
          - refs/tags/v*

pool:
  vmImage: 'ubuntu-latest'

variables:
  - template: project.yml

extends:
  template: azure/common/apigee-deployment.yml@common
  parameters:
    service_name: ${{ variables.service_name }}
    short_service_name: ${{ variables.short_service_name }}
    service_base_path: ${{ variables.service_base_path }}
    product_display_name: ${{ variables.product_display_name }}
    product_description: ${{ variables.product_description }}
    spec_file: ${{ variables.spec_file }}
    secret_ids:
     - ptl/azure-devops/hello-world-env-internal-dev/API_KEY
    apigee_deployments:
      - environment: internal-dev
      - environment: internal-dev-sandbox
      - environment: internal-qa-sandbox
      - environment: sandbox
        depends_on:
          - internal_dev_sandbox
          - internal_qa_sandbox
    post_deploy:
        - template: templates/postman-tests.yml
          parameters:
            service_name: ${{ variables.service_name }}
            base_path: ${{ variables.service_base_path }}
            environments:
              - internal-dev
